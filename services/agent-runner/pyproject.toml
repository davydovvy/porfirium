[project]
name = "porfirium-agent-runner"
version = "0.2.0"
description = "Agent Runner service"
requires-python = ">=3.12"
dependencies = [
  "asyncpg==0.30.0",
  "cryptography==45.0.7",
  "fastapi==0.116.1",
  "httpx==0.28.1",
  "nats-py==2.15.0",
  "uvicorn==0.35.0",
]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_runner"]

[dependency-groups]
dev = [
  "pytest==8.4.2",
  "ruff==0.12.11",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
