[project]
name = "genai-platform-time-mcp"
version = "0.1.0"
description = "Read-only deterministic time tools for Porfirium"
requires-python = ">=3.12"
dependencies = [
  "mcp>=1.20,<2",
  "starlette>=0.47,<1",
  "uvicorn>=0.35,<1",
]

[tool.uv]
package = false
